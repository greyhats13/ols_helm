# Default values for product.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: greyhats13/product
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: alpha

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
appConfig:
  DB_HOST: mysql.db.svc.cluster.local
  DB_NAME: ols_product
  DB_USER: product
  DB_PORT: 3306
  SQLALCHEMY_TRACK_MODIFICATIONS: False
  SQLALCHEMY_RECORD_QUERIES: True
  CACHE_TYPE: redis
  CACHE_REDIS_HOST: redis.db.svc.cluster.local
  CACHE_REDIS_PORT: 6379
  CACHE_REDIS_DB: 0
  CACHE_DEFAULT_TIMEOUT: 300
  FLASK_APP: app.py
  FLASK_ENV: development
  FLASK_DEBUG: True
  FLASK_RUN_PORT: 5000
  FLASK_RUN_HOST: 0.0.0.0
appSecret:
  DB_PASSWORD: pass
  CACHE_REDIS_PASSWORD: pass


serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 5000

ingress:
  enabled: enabled
  className: "alpha"
  annotations:
    # kubernetes.io/ingress.class: alpha
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: product.api.dev.ols.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 257
  # targetCPUUtilizationPercentage: 75
  # targetMemoryUtilizationPercentage: 75

nodeSelector: {}

tolerations: []

affinity: {}
